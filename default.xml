<?xml version="1.0"?>
<!DOCTYPE benchmark PUBLIC "+//IDN sosy-lab.org//DTD BenchExec benchmark 1.0//EN" "http://www.sosy-lab.org/benchexec/benchmark-1.0.dtd">
<benchmark tool="ceagle_absref">
    <rundefinition name="Default">
    </rundefinition>

    <tasks name="bitvectors">
        <includesfile>BitVectors.set</includesfile>
    </tasks>

    <tasks name="floats">
        <includesfile>Floats.set</includesfile>
    </tasks>

    <tasks name="recursive">
        <includesfile>Recursive.set</includesfile>
    </tasks>

    <propertyfile>ALL.prp</propertyfile>

</benchmark>
